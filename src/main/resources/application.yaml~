spring:
  application:
    name: ci-test-1

  datasource:
    url: jdbc:mysql://localhost:3306/kafkademo
    username: root
    password: jamil
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    org.mdzahidhasanjamil.citest1: DEBUG
